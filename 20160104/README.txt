This is a project from Jiajia.

The origin data is the record of patients' appointment of a clinic, which is in .xsl format.
The main task is to calculate the "effective rate" of the calls after working hour, which is preset to 17:30.

The implemention is based on python, and the original data is saved to .csv format (by excel).